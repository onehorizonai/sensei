interface:
  display_name: "Sensei Align"
  short_description: "Check whether a code change follows local patterns and conventions."
  default_prompt: "Use $sensei-align to check if this change follows our existing patterns."
